# Task List — เว็บแนะนำอาหารไทย

อ้างอิงศัพท์จาก [CONTEXT.md](./CONTEXT.md) และข้อตกลงเรื่อง slug จาก
[ADR-0001](./docs/adr/0001-stable-slug-ids-for-favourites.md)

เรียงตามลำดับที่ควรทำ แต่ละข้อจบแล้วควรเปิดไฟล์ดูผลได้จริง

## 1. โครงไฟล์

- [x] สร้าง `index.html` โครงเปล่า ผูก `style.css` และ `app.js` ตั้ง `<title>` เป็น
      "วันนี้กินอะไรดี — แนะนำอาหารไทย" ตั้ง `lang="th"` และ viewport meta
- [x] วาง section ตามลำดับ: หัวเรื่อง → การ์ด Featured Dish → ปุ่มสุ่มใหม่ →
      แถบตัวกรอง → กริด Pool → ฟุตเตอร์
- [x] ไม่แตะไฟล์ว่างชื่อ `code` ที่ root

## 2. ข้อมูล Pool

- [x] เขียนอาเรย์ `DISHES` ใน `app.js` 30 จาน ฟิลด์ `slug`, `name`, `nameEn`,
      `desc`, `emoji`, `category`, `spicy` (0-3)
- [x] สัดส่วน Category: จานเดียว 12 · กับข้าว 9 · ยำ-น้ำพริก 5 · ของหวาน 4
- [x] เลือกจานที่คนสั่งกินจริงในชีวิตประจำวันเป็นหลัก ไม่ใช่จานดังที่หาซื้อยาก
- [x] ตรวจว่า slug ไม่ซ้ำกันและเป็นอังกฤษ kebab-case ทั้งหมด

## 3. ระบบสีและธีม

- [x] ตั้ง CSS custom properties ชุดสว่าง แล้ว override ใต้
      `@media (prefers-color-scheme: dark)` — ไม่มีปุ่มสลับธีม
- [x] กำหนดสีประจำ 4 Category ใช้กับ**ชิปเล็ก**เท่านั้น ไม่ใช้เป็นพื้นการ์ดเต็มใบ
- [x] ตรวจคอนทราสต์ตัวอักษรให้ผ่านทั้งโหมดสว่างและมืด

## 4. การ์ด Featured Dish

- [x] เรนเดอร์การ์ดใบใหญ่: emoji, ชื่อไทย, ชื่ออังกฤษ, `desc`, ชิป Category,
      พริกตามค่า `spicy`, ปุ่มหัวใจ
- [x] ใส่ `aria-live="polite"` ที่การ์ด เพื่อให้โปรแกรมอ่านหน้าจอประกาศจานใหม่
- [x] สุ่มหนึ่งจานทันทีตอนโหลดหน้า ไม่ต้องรอผู้ใช้กด

## 5. ปุ่มสุ่มใหม่

- [x] วางปุ่ม "สุ่มใหม่" ไว้**ใต้**การ์ด
- [x] สุ่มจาก Pool ทั้งหมดเสมอ — ไม่สนชิป Category และไม่สนสวิตช์เฉพาะที่ชอบ
- [x] กันไม่ให้ได้จานล่าสุดซ้ำติดกัน โดยจำแค่ 1 จานไว้ในตัวแปร ไม่เก็บลง localStorage
- [x] ทรานซิชันเปลี่ยนการ์ด ~200ms และข้ามทรานซิชันเมื่อ `prefers-reduced-motion: reduce`

## 6. กริด Pool

- [x] เรนเดอร์การ์ด 30 ใบ: emoji, ชื่อไทย, ชื่ออังกฤษ, พริก, ชิป Category, ปุ่มหัวใจ
      — **ไม่ใส่** `desc`
- [x] การ์ดสูงเท่ากันทุกใบ กริด 1 คอลัมน์บนมือถือ → 2 → 3-4 บนจอกว้าง (mobile-first)
- [x] กดที่ตัวการ์ด (ไม่ใช่ที่หัวใจ) แล้วส่งจานนั้นขึ้นไปเป็น Featured Dish
- [x] การ์ดที่ตรงกับ Featured Dish ปัจจุบันขึ้นขอบเน้น — ห้ามเลื่อนจออัตโนมัติไปหา

## 7. Favourite และ localStorage

- [x] ปุ่มหัวใจเป็น `<button aria-pressed>` พร้อม `aria-label` ระบุชื่อจาน
      เช่น "ชอบ ผัดกะเพรา" — กด Tab/Enter ได้
- [x] อ่าน/เขียน key `thai-food:favourites` เป็น JSON array ของ slug
- [x] ห่อทั้ง read และ write ด้วย try/catch — พังแล้วเว็บทำงานต่อได้ปกติ ไม่แจ้งเตือนผู้ใช้
- [x] ตอนอ่าน ให้ตัด slug ที่ไม่มีอยู่ใน Pool ปัจจุบันทิ้ง (ADR-0001)
- [x] หัวใจของจานเดียวกันบนการ์ดใบใหญ่และในกริดต้องเปลี่ยนสถานะพร้อมกัน
- [x] ไม่ต้องดัก `storage` event — ไม่ sync ข้ามแท็บ

## 8. ตัวกรอง

- [x] ชิป Category เลือกได้ทีละหนึ่ง มีชิป "ทั้งหมด" เป็นค่าเริ่มต้น
- [x] สวิตช์ "เฉพาะที่ชอบ" รวมกับชิป Category เป็นเงื่อนไข AND
- [x] เอาหัวใจออกขณะเปิดสวิตช์เฉพาะที่ชอบ — การ์ดต้อง**ค้างอยู่** ไม่หายวับ
      จนกว่าจะสลับตัวกรองใหม่
- [x] ข้อความตอนผลกรองว่าง ระบุสาเหตุตามตัวกรองที่เปิดอยู่ เช่น
      "ยังไม่มีของหวานที่ชอบ" และกรณียังไม่เคยกดหัวใจเลยให้ชวนกดพร้อมปุ่มปิดตัวกรอง

## 9. ฟุตเตอร์

- [x] บรรทัดเดียว: จำนวนจานในลิสต์ + ระดับความเผ็ดเป็นการประมาณ +
      รายการโปรดเก็บไว้ในเครื่องนี้เท่านั้น
- [x] ไม่ใส่เครดิตหรือปีที่ทำ

## 10. ตรวจงานก่อนส่ง

- [ ] เปิด `index.html` ด้วย double-click แล้วใช้งานได้ครบโดยไม่ต้องมี server
      — **ข้อเดียวที่ยังไม่ได้ทดสอบจริง** ส่วนขยายเบราว์เซอร์เปิด `file://` ไม่ได้
      จึงทดสอบผ่าน localhost แทน (โค้ดไม่มี `fetch` และไม่ใช้ ES module จึงไม่ติดข้อจำกัดของ
      `file://` แต่ยังต้องให้คุณ double-click ยืนยันเอง)
- [x] กดหัวใจ → ปิดเบราว์เซอร์ → เปิดใหม่ แล้ว Favourite ยังอยู่
- [x] ลองลบจานหนึ่งออกจาก `DISHES` ทั้งที่เคยกดหัวใจไว้ → เว็บต้องไม่พังและสถานะนั้นหายไป
- [x] ไล่กดปุ่มสุ่มรัว ๆ แล้วไม่เจอจานเดิมติดกัน
- [x] ลองปิด localStorage (โหมดส่วนตัว) แล้วเว็บยังใช้งานได้ หัวใจแค่ไม่ถูกจำ
- [x] ตรวจบนความกว้างจอมือถือว่าไม่มีสกรอลล์แนวนอน
- [x] ไล่ด้วยคีย์บอร์ดอย่างเดียวจนครบทุกปุ่ม

## 11. README

- [x] เขียน `README.md`: วิธีเปิด (double-click `index.html`) และวิธีเพิ่มจานใหม่ลง `DISHES`
- [x] เตือนตัวหนา: **ห้ามแก้ slug ของจานที่มีอยู่แล้ว** เพราะจะทำให้ Favourite
      ของคนที่เคยกดไว้หาย (ADR-0001)

---

## ผลการทดสอบ (รันจริงใน Chrome ผ่าน localhost)

| ทดสอบ | ผล |
|---|---|
| ข้อมูล 30 จาน slug ไม่ซ้ำ รูปแบบ kebab-case | ผ่าน — สัดส่วน 12/9/5/4 ตรงตามที่ตกลง |
| กดสุ่ม 40 ครั้ง | ไม่เจอจานเดิมติดกันเลย เจอ 23 จานต่างกัน |
| หัวใจบนการ์ดใบใหญ่ ↔ หัวใจในกริด | เปลี่ยนพร้อมกันทั้งสองทิศทาง |
| slug ที่ไม่มีใน Pool ค้างอยู่ใน localStorage | ถูกตัดทิ้งตอนอ่าน เว็บไม่พัง (ADR-0001) |
| ข้อมูลใน localStorage เป็น JSON พัง | ถือว่าไม่มี Favourite เรนเดอร์ครบ 30 การ์ดตามปกติ |
| `setItem` โยน error (จำลองโหมดส่วนตัว) | ไม่หลุดออกมาถึงผู้ใช้ หัวใจยังเปลี่ยนหน้าตาได้ เว็บใช้งานต่อได้ |
| ชิป Category + สวิตช์เฉพาะที่ชอบ | เป็น AND ถูกต้อง |
| ผลกรองว่าง | ขึ้น "ยังไม่มีของหวานที่ชอบ" พร้อมปุ่มปิดตัวกรองที่กดแล้วได้ผล |
| เอาหัวใจออกขณะกรองอยู่ | การ์ดค้างไว้ หายเมื่อสลับตัวกรองใหม่เท่านั้น |
| ปุ่มสุ่มขณะกรองเหลือ 4 จาน | ยังสุ่มได้จานนอกกลุ่ม = ไม่สนตัวกรองตามที่ตกลง |
| กดการ์ดในกริด | ขึ้นเป็น Featured Dish และขอบเน้นย้ายตาม ไม่มีการเลื่อนจออัตโนมัติ |
| ความกว้าง 390 / 600 / 820 / 1100 px | กริด 1 / 2 / 3 / 4 คอลัมน์ ไม่มีสกรอลล์แนวนอน การ์ดสูงเท่ากันทุกใบ |
| คีย์บอร์ด | โฟกัสได้ 68 จุด เรียง หัวใจใบใหญ่ → ปุ่มสุ่ม → ชิป → สวิตช์ → กริด |
| ARIA | หัวใจเป็น `<button aria-pressed>` มี label ระบุชื่อจาน · การ์ดใบใหญ่ `aria-live="polite"` · พริกมี label "ความเผ็ด n จาก 3" |
| console ตอนโหลด | ไม่มี error หรือ warning |
| โหมดมืด | เรนเดอร์ด้วยชุดสีมืดจริงแล้วอ่านออกทั้งหน้า |
