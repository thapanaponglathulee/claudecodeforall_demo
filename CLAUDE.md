# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Running the app

Vite + Redux Toolkit ไม่มี test runner

```bash
npm install        # ครั้งแรกครั้งเดียว
npm run dev        # dev server
npm run build      # ออกไฟล์ลง dist/
```

**เปิด `index.html` ตรง ๆ ไม่ได้แล้ว** ต้องผ่าน dev server เท่านั้น ทั้งเพราะเป็น ES module
และเพราะ `localStorage` ทำงานต่างกันบน `file://` (บางเบราว์เซอร์ถือเป็น opaque origin
แล้ว throw) ซึ่งเป็นที่เก็บ Order ทั้งหมด

ไม่มี automated test — รายการตรวจด้วยมืออยู่ใน `task.md`

## หน้าตาของเว็บมาจาก DESIGN.md

`DESIGN.md` คือ style reference ที่สกัดมาจาก Hartzler Family Dairy และเป็นตัวตัดสิน
เรื่องหน้าตาทั้งหมด **อ่านก่อนแตะ CSS ทุกครั้ง** กฎที่พังบ่อยถ้าไม่ได้อ่าน:

- **ห้ามมีเงาเลย** ไม่ว่า `box-shadow` หรือ `text-shadow` ความลึกมาจากขนาดตัวอักษร
  และที่ว่าง ไม่ใช่จาก CSS
- **ปุ่มเป็นเส้นขอบล้วน** ห้ามถมพื้นด้วย `#035542` หรือ `#2b7bb9` สถานะ "ถูกเลือก"
  ทำด้วยการเพิ่มความหนาเส้นขอบ ไม่ใช่การถมสี
- **border-radius มีแค่ 4 ค่า** — 0 (tag) · 4px (body) · 20px (ปุ่ม) · 100px (pill)
  ห้าม 8px 12px 50px
- **teal `#56dddb` กับ butter yellow `#f9e9a9` ใช้ได้เฉพาะตัวอักษร billboard เท่านั้น**
  ห้ามแตะ UI ที่ต่ำกว่า 40px เพราะคอนทราสต์บนพื้นขาวต่ำเกินไป
- **ธีมเดียวคือธีมสว่าง** พื้นขาวล้วน ไม่มี gradient ไม่มีลวดลาย

คำ billboard ประจำหมวดอยู่ในฟิลด์ `word` ของ `CATEGORIES` เป็นคำไทยถอดเป็นอักษรโรมัน
เพราะ line-height 0.70 ตามสเปททำให้สระบนและวรรณยุกต์ของตัวไทยทับกันจนอ่านไม่ออก
คำพวกนี้เป็นงานภาพล้วน จึงใส่ `aria-hidden` ไว้ ชื่อหมวดจริงภาษาไทยอยู่ใน `<h3 class="tag">`

## ภาษาที่ใช้ในโค้ดเบสนี้

คอมเมนต์ เอกสาร ข้อความ UI และ commit message ที่มีอยู่เป็นภาษาไทย ยกเว้นตัวระบุใน
โค้ด (ชื่อฟังก์ชัน ตัวแปร `slug`) ที่เป็นอังกฤษ — เขียนของใหม่ให้เข้ากับรูปแบบนี้

ศัพท์ที่ใช้เรียกสิ่งต่าง ๆ นิยามไว้แล้วใน `CONTEXT.md` (Item, Menu, Category,
Recommended, Order, Total, Shop) พร้อมคำที่ตั้งใจเลี่ยง — ใช้คำตามนั้นทั้งในโค้ดและ
ในการสนทนา อย่าคิดคำใหม่ขึ้นมาเอง

## สถาปัตยกรรม

```
src/data/menu.js     MENU, CATEGORIES, SHOP — ข้อมูลดิบทั้งหมด
src/store/           Redux Toolkit
  orderSlice.js        รายการที่เลือก + selector คำนวณยอด
  filtersSlice.js      คำค้น หมวด สวิตช์แนะนำ + selector กรองเมนู
  uiSlice.js           nav, แผงตะกร้า, preview ที่เปิดอยู่
  storage.js           อ่าน/เขียน localStorage
  index.js             configureStore + listener middleware ที่เขียนลงเครื่อง
src/ui/              ฟังก์ชันวาดหน้าจอ ไม่รู้จัก store
src/main.js          ผูก store เข้ากับหน้าจอ และผูก event ทั้งหมด
```

`src/ui/` **ไม่ import store** รับค่าที่ต้องใช้มาเป็นพารามิเตอร์อย่างเดียว ส่วน `main.js`
เป็นที่เดียวที่ `dispatch` และอ่าน state

**ห้ามเรียก `saveOrder()` เองจากที่อื่น** — listener middleware ใน `store/index.js`
เขียนลง localStorage ให้แล้วเมื่อมี action ของ order เข้ามา มีที่เขียนลงเครื่องแค่ที่เดียว

**`MENU` คือแหล่งข้อมูลเดียวของทั้งเว็บ** — การค้นหา ตัวกรอง ตัวเลขข้างชื่อหมวด
และจำนวนเมนูในฟุตเตอร์ ล้วนคำนวณจากอาเรย์นี้ เพิ่มหรือลบเมนูแล้วทุกอย่างตามให้เอง
`BY_SLUG` เป็น Map ที่สร้างจาก `MENU` ไว้ค้นด้วย slug

`SHOP` เก็บข้อมูลร้านที่ไม่ขึ้นกับเมนู ทั้งคู่เป็นสแนปช็อตที่คัดลอกจาก Wongnai
เมื่อ 2026-09-20 ไม่ได้ดึงสด **ไม่ต้องพยายามทำให้ดึงสด** — หน้าเว็บนี้ไม่มี backend
และการยิงตรงจากเบราว์เซอร์ไปหน้าคนอื่นติด CORS อยู่แล้ว

การเรนเดอร์เป็น string template ยัดเข้า `innerHTML` ข้อมูลเมนูคัดลอกมาจากหน้าเว็บ
คนอื่น ไม่ใช่ข้อความที่เราพิมพ์เองทั้งหมด จึง **escape ด้วย `esc()` ทุกครั้ง**
ก่อนแทรกลง HTML

สถานะทั้งหมดอยู่ใน store ไม่มีตัวแปรสถานะลอย ๆ นอก store เลย ยกเว้น `prev` ใน
`main.js` ที่เก็บค่ารอบก่อนไว้เทียบว่าต้องวาดอะไรใหม่

## ข้อตกลงที่ห้ามพังโดยไม่ตั้งใจ

**ห้ามแก้ `slug` ของเมนูที่มีอยู่แล้ว** — `localStorage` ในเครื่องผู้ใช้เก็บแต่ slug
กับจำนวน เปลี่ยนเมื่อไหร่ Order ที่ค้างไว้หายทันทีและหายเงียบ ๆ แก้ `name` กับ `price`
ได้ตามสบาย (ADR-0001)

**กดเพิ่ม/ลดจำนวนแล้วต้องไม่เรนเดอร์ลิสต์ใหม่ทั้งหมด** — ข้อนี้เปราะที่สุดในโค้ดเบสนี้
เพราะ `store.subscribe` ยิงทุก action ตัวที่กันไว้คือ `sync()` ใน `main.js`
ซึ่งเทียบ `selectVisibleByCategory` ด้วย `!==` ได้ เพราะ `createSelector` คืน
reference เดิมเมื่อตัวกรองไม่เปลี่ยน ถ้าเปลี่ยนเฉพาะจำนวนจะลงไปวาดแค่การ์ดที่ขยับ

แก้ `sync()` เมื่อไหร่ **ต้องตรวจซ้ำด้วยมือ** ว่ากดเพิ่มจำนวนแล้วมีการ์ดถูกวาดใหม่ใบเดียว
จริง วิธีตรวจ: ปักหมุด `document.querySelectorAll('.card').forEach((c,i)=>c.__mark=i)`
กด `+` แล้วนับว่ามีกี่ใบที่ `__mark` หาย ต้องได้ 1

**รูปมีสองขนาด อย่าเดาขนาดที่สาม** — การ์ดใช้ `256x256` กล่อง preview ใช้ `800x0`
Wongnai เสิร์ฟเฉพาะบางขนาด (`600x600` กับ `960x0` คืน 404) ถ้าจะเปลี่ยนขนาดต้องยิงลองก่อน

**รูปจาก Wongnai ต้องมีทางลงเสมอ** — เราไม่ได้เป็นเจ้าของไฟล์ รูปหายได้ทุกเมื่อ
จึงดัก `error` ของ `<img>` แบบ capture (error ไม่ bubble) แล้วสลับไปแสดง emoji
ประจำหมวด ห้ามปล่อยให้เหลือกรอบรูปแตก (ADR-0002)

**localStorage ที่พังต้องเงียบ** — ทั้ง `loadOrder()` และ `saveOrder()` กลืน error ทิ้ง
เบราว์เซอร์ที่ปิด storage ต้องใช้เว็บได้ปกติ แค่จำ Order ข้ามครั้งไม่ได้
`loadOrder()` ยังตัด slug ที่ไม่มีใน `MENU` ปัจจุบันและจำนวนที่ไม่สมเหตุสมผลทิ้งด้วย

**หน้านี้ต้องไม่ทำตัวเป็นหน้าสั่งอาหารของร้าน** — ไม่มีปุ่มที่ส่งข้อมูลออกไปไหน
คำเตือนในฟุตเตอร์ที่บอกว่านี่ไม่ใช่หน้าของร้านต้องอยู่ต่อไป และปุ่มสั่งจริงต้องลิงก์
ออกไปหน้า LINE MAN ของร้านเท่านั้น

## กับดักเวลาแก้ไฟล์นี้บน Windows

Bash tool ในสภาพแวดล้อมนี้ **กินแบ็กสแลชใน heredoc** — เขียน `/\s+/g` ผ่าน
`cat > file <<'EOF'` แล้วจะกลายเป็น `/s+/g` ซึ่งไปลบตัวอักษร `s` ทุกตัวในข้อมูลเงียบ ๆ
(เคยทำ slug เพี้ยนจาก `soft-cookie` เป็น `oft-cookie` มาแล้ว)

เวลาสร้างหรือแก้ไฟล์ที่มี regex หรือ escape sequence ให้ใช้ Write/Edit tool
อย่าใช้ heredoc
